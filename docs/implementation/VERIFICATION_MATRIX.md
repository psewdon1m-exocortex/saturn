# Verification matrix

## Stage status and evidence

| Stage | State | Required evidence | Current evidence |
| --- | --- | --- | --- |
| 0 Contracts | `COMPLETE` | Required documents, section checks, ignore rules | 8 required files present; 14 stages documented; state model and secret ignore rule verified on 2026-08-25 |
| 1 Storage qualification | `COMPLETE` | Capability report, measurements, cleanup | 20 GiB report passed 10/10; SHA-256 round trip; 9.996/6.484 MiB/s; 200.66 MiB RSS; pool 8; cleanup and empty listing confirmed |
| 2 Foundation | `COMPLETE` | Builds, tests, migrations, health, secret scan | Aggregate 13/13 PASS; 11 tests; build; migration rollback; DB/SFTP failure recovery; 100-file exact-secret scan; cleanup confirmed |
| 3 File core | `COMPLETE` | Unit/integration/live storage results | Aggregate 13/13 PASS; 22 tests; migration round trip; auth denial; restart/resume; SHA-256/Range; stable move/copy/trash; live DEV pool 8/truncate/cleanup |
| 4 Protection/audit | `COMPLETE` | Restore, reconciliation, retention, redaction | Aggregate 10/10 PASS; 29 tests; overwrite/version/trash restore; append-only trigger; redacted export; purge disabled; mismatch fixtures; live DEV cleanup |
| 5 Saturn recovery | `COMPLETE` | Clean restore, rollback and owner-web evidence | Aggregate 9/9 baseline PASS plus live owner-web snapshot/upload/validation/replace on 2026-09-03; 95,080-byte ZIP; exact digest; isolated and live restore; 24 resources/16 versions/151 audit events/23 migrations; 1,843 ms DEV RTO; readiness `ok`; upload journal cleanup confirmed |
| 6 Owner UI | `COMPLETE` | Auth/API/browser/accessibility evidence | Aggregate 11/11 PASS; migration round trip; cookie session/CSRF/reauth/revoke/rate-limit; complete browser file workflow; forged preview rejection; 1440/320 screenshots; zero serious/critical Axe findings; browser storage and secret scans clean |
| 7 Telegram/Drop | `COMPLETE` | Provider, concurrency and privilege tests | Aggregate 16/16 PASS; migration round trip; Telegram validation/link/replay/revoke; purpose-bound codes; restart/resume checksum; upload-only matrix; atomic quota/rate limits; browser 1440/320 and zero serious/critical Axe findings; log/artifact/backup scans clean |
| 8 Sharing | `COMPLETE` | Public-route negative tests and Range E2E | Aggregate 12/12 PASS; migration round trip; 256-bit one-time token; Argon2id/rate limit; exact Range and stable move; concurrent max-download; revoke/expiry/trash/classification and active-stream denial; bounded ZIP/cleanup; browser 1440/320 and zero serious/critical Axe findings; log/audit/export/backup/artifact scans clean |
| 9 Sync/KeePass | `COMPLETE` | Real-client, conflict and recovery tests | Aggregate 14/14 PASS; 63 tests/build; migration round trip; one-time scoped tokens; traversal containment; exact ETag/Range; conflicts/versions; mass-delete bound; real rclone 1.75.0; complete `.obsidian` export; real KDBX4 current/old recovery; browser 1440/320 and zero serious/critical Axe findings; secret/export/backup/artifact scans clean |
| 10 Backup ingest | `COMPLETE` | Isolation, quota, checksum and restore tests | Aggregate 14/14 PASS; 67 tests/build; migration rollback; restart/resume and receipt; two-producer upload-only matrix; chunk/checksum cleanup; atomic daily/concurrent quota races; optional mTLS; GFS preview; generated isolated restore and integrity evidence; rotation/revoke independence; browser 1440/320 and zero serious/critical Axe findings; secret/recovery/artifact scans clean |
| 12 Laboratory | `COMPLETE` | Stable-link, classification and cache tests | Aggregate 12/12 PASS; 76 tests/build; migration rollback; private-by-default and HMAC client scope; immutable/alias overwrite; stable rename; HEAD/Range/ETag/cache; pinned purge exemption; denial matrix; browser 1440/320 with zero serious/critical Axe findings and token-free evidence; recovery/audit/secret scans clean; report SHA-256 `c43f64c48b75c29449318439a37ece8c2101e43a2e411874cc0979f56a1ab43f` |
| Storage layout v2 / root policy | `COMPLETE` | Physical/SQL migration, stable-ID root policy, rollback, fail-closed preflight and Gateway smoke | Six visible rename-only preinstalled roots plus arbitrary full-lifecycle folders and hidden `_system`; no direct root files or `drive`; generation 14, layout metadata v2 and root-policy metadata v1; SQL `up -> down -> up`; idempotent physical up preserving arbitrary/renamed roots; collision/direct-file/unclassified legacy rejection; local HTTP API policy smoke and clean root; canonical DEV origin permits CSRF-protected folder creation without invalidating the session; real DEV Storage Box retains the verified `STORAGE_ROOT=.` layout and prior clean 65,573-byte SFTP checksum/delete evidence; aggregate lint, typecheck, 80 tests and production builds passed on 2026-09-01 |
| 13 Production | `BLOCKED` | Deployment, rollback, DR, external-vantage and exit | Local aggregate 12/12 PASS: 83 tests/build; hardened non-root images; file-only secrets; edge-only topology; pinned tag-only CI and signed release/SBOM; canonical bootstrap and exact smoke cleanup; HTTPS Gateway E2E; SFTP/DB outage recovery; fail-closed bad candidate without traffic switch; clean-host DB restore; alternative StorageAdapter exit; zero secret findings. Real PROD sub-account/domain/host/second copy and unauthorized external-vantage evidence remain operator-owned blockers; see `PRODUCTION_READINESS.md` |
| 14 Storage/Share/Drop UX | `COMPLETE_WITH_ISOLATED_DECISIONS` | UI contract, buffered states, checksum, folder ZIP, public Share and browser evidence | Aggregate lint/typecheck/114 tests/build PASS on 2026-09-03; migrations 0020 and 0022 applied in DEV, with isolated 0022 up/down/up; real two-client same-code HTTP and SSE queue smoke, common issue-anchored 30-minute expiry, peer cancellation and cross-channel `401`; real SFTP `buffered -> stored` and owner/public SHA-256; password/expiry/descendant/package/revoke E2E; folder ZIP exact bytes; login paste and 390px public browser checks. Historical capability reissue and root-file upload remain isolated policy decisions. |
| 15 Runtime storage switching | `COMPLETE_LOCAL` | Protected UI/API, pinned candidate preflight, catalog replacement, API/worker convergence, rollback and zero-migration evidence | Aggregate lint, 20 TypeScript projects, 122 Vitest tests and all 19 production builds pass on 2026-09-04; Compose renders with the production example; live DEV status and write-only connection test report reachable SFTP plus capacity without credential fields; isolated `verify:stage:15` passes migration up/down/up and two consecutive switches of a disposable DB across temporary SFTP roots; final target: 1 exact file, 7 directories and 75 bytes; previous target preserved; 0 bytes migrated; secret-free runtime metadata and clean fixture teardown; exact scan across 316 source/config/docs files finds zero protected DEV secrets. Production activation is intentionally not performed by this verifier. |

## Cross-layer verification rule

Every numeric limit and security state must be traced through all four layers:

| Requirement | Configuration | Runtime enforcement | UI/docs | Automated test |
| --- | --- | --- | --- | --- |
| Upload size | `UPLOAD_MAX_BYTES=20 GiB`, `UPLOAD_CHUNK_MAX_BYTES=8 MiB` | `FileService` rejects larger sessions/chunks and the API body limit matches the chunk ceiling | Stage 3 operator contract and Stage 6 chunked owner UI | Unit rejection, aggregate API path and browser upload/overwrite in Stages 3 and 6 |
| Drop TTL/batch/buffer | 5-minute link code; Drop-code admission, shared channel and all client sessions use one absolute expiry 30 minutes after code issue; 1,000 files/100 GiB per channel, 110 GiB buffer, 24-hour bounded continuation, two workers (hard maximum four), 5/source and 100/global failed attempts | Transactional challenges/channels/client sessions, non-extending late redemption, channel-atomic quota reservations, exact-offset local staging, SSE snapshots, watermarks, durable queue claims and remote checksum before `stored` | Stage 7 capability contract plus Stage 14 buffered public/in-house UI | Purpose/absolute expiry, concurrent same-code clients, cross-channel/tab isolation, real-time peer snapshots, quota/rate, resume/cancel, watermarks, worker claims and real DEV `buffered -> stored` checksum E2E |
| Share expiry/downloads | 7-day/365-day expiry, 30-minute session, 5 attempts/15 minutes, bounded ZIP and stream revalidation in validated config | HMAC capabilities, Argon2id proof, transactional claim, stable resource policy checks and worker cleanup | Stage 8 owner/public UI and external-sharing contract | Aggregate token/rate/Range/concurrency/revoke/package/browser and secret-scan E2E |
| Device path scopes | Explicit stable-root IDs, independent rights, 1,000-entry PROPFIND cap, four items/15-minute DEV delete window | Single-decode logical resolver, same-scope destinations, ETag/If-Match, independently revocable HMAC tokens and transactional delete reservation | Settings device form and Stage 9 protocol/rollback contract | Unit path/token/conflict tests plus aggregate traversal, rclone, expiry/revoke and mass-delete E2E |
| Service backup quota | Per-service run/daily/stored/concurrent limits with encrypted-by-default ingest and 64 KiB–8 MiB bounded chunks | Service-row transactional reservations derive namespace from HMAC-authenticated identity; producer API has no list/read/delete/retention routes | Settings producer dashboard and Stage 10 protocol/rollback contract | Unit token/resume/retention tests plus aggregate restart, cross-service, checksum, quota-race, mTLS, restore and browser E2E |
| Laboratory delivery | Public disabled by default, one Range, 256-bit client tokens, bounded concurrent public streams and bounded rotation overlap | HMAC client verification, exact stable filename, public classification, immutable pin/alias selection and purge exclusion | Files/Laboratory owner workflows and Stage 12 contract | Unit scope/cache/limit tests plus aggregate token, overwrite/move, HTTP, denial, browser, recovery and secret-scan E2E |
| Audit retention | Bounded 500/page and 100,000/export; append-only DB | Activity/export APIs and recursive redactor | Stage 4 operator contract and Stage 6 Activity UI | Immutability, secret corpus, bounds/export tests and browser activity workflow in Stages 4 and 6 |
| Backup archive bounds | Archive/member/extracted/entry/ratio/manifest limits in validated recovery config | Two-pass central-directory/manifest validation, streaming digest, bounded exact-offset upload, exclusive write barrier and transactional replace | Stage 5 operator contract and Settings → Backup workflow | Traversal, duplicate, corrupt, unknown, digest, member-size, ratio, pre-mutation and spool-cleanup tests plus isolated and live owner-web restore |

## Required final evidence set

- applicability matrix with evidence for exclusions;
- baseline and material-divergence decisions;
- storage qualification report;
- build, unit, integration and browser results;
- secret and artifact scans;
- backup/restore and failed-restore rollback report;
- deployment and update rollback report;
- exposure tests from authorized and unauthorized vantage points;
- recovery drill and alternative-backend exit report;
- residual-risk handoff.
